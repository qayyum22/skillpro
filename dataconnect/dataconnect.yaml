specVersion: "v1beta"
serviceId: "skillpro"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "skillpro-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
